const Menu = () => {
  return (
    <div className="flex flex-col justify-between items-center ">
      <button>Start</button>
      <button>Change name</button>
    </div>
  );
};

export default Menu;
